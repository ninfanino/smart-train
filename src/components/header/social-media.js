import React from 'react'
import IcomoonReact from 'icomoon-react'
import iconSet from '../selection.json'

const SocialMedia = () => {
    <div>
        <IcomoonReact iconSet={iconSet} size={60} icon="facebook" color="#000" />
    </div>
}

export default SocialMedia;